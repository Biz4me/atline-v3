import React from 'react';
import type { ServerFunctionClient } from 'payload';

/* THIS FILE IS REQUIRED — do not delete */
/* Payload admin layout wrapper */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
